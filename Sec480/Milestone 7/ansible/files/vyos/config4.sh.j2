TYPE=Ethernet
PROXY_METHOD=none
BROSWER_ONLY=no
BOOTPROTO=none
DEFROUTE=yes
IPV4_FAILURE_FATAL=no
IPV6INT=no
NAME="{{ device }}"
DEVICE="{{ device }}"
ONBOOT=yes
IPADDR="{{ lan_ip }}"
PREFIX="{{ prefix }}"
GATEWAY="{{ gateway }}"
DNS1="{{ name_server }}"
DOMAIN="{{ domain }}"